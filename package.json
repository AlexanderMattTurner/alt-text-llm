{
  "name": "alt-text-llm",
  "private": true,
  "description": "Node manifest for CI only. This is a Python package; nothing here ships. The pre-commit workflow synced from claude-automation-template runs `pnpm install` before its hooks, and pnpm exits non-zero with no manifest to read.",
  "packageManager": "pnpm@10.28.2"
}
